require 'spec_helper'

describe FubClient do
  it 'has a version number' do
    expect(FubClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
